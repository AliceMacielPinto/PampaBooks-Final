<!-- views/formulario-livro.ejs -->
<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Cadastro de Livro</title>
  <style>
   * {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
  font-family: Arial, sans-serif;
}

body {
  background-color: #f4f4f9;
  color: #333;
  min-height: 100vh;
  display: flex;
  flex-direction: column;
  justify-content: flex-start; /* Ajusta a posição do conteúdo */
}

.menu {
  display: flex;
  justify-content: space-between;
  align-items: center;
  background-color: #f8f9fa; /* Cor clara */
  padding: 15px 20px;
  box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1); /* Sombras suaves */
  border-bottom: 2px solid #e9ecef;
  position: fixed;
  top: 0;
  width: 100%;
  z-index: 1000; /* Garantir que o menu fique acima de outros conteúdos */
}

.menu div {
  display: flex;
  align-items: center;
}

.menu a {
  color: #4CAF50; /* Cor verde para contraste */
  text-decoration: none;
  margin-left: 20px;
  font-weight: bold;
  font-size: 1rem;
  padding: 8px 12px;
  border-radius: 5px;
  transition: background-color 0.3s ease, color 0.3s ease;
}

.menu a:first-child {
  margin-left: 0;
}

.menu a:hover {
  background-color: #4CAF50; /* Fundo verde ao passar o mouse */
  color: white; /* Texto branco */
}

h1 {
  margin-top: 80px; /* Espaço para o menu fixo */
  margin-bottom: 20px;
  font-size: 2rem;
  color: #555;
  text-align: center; /* Centraliza o título */
}

form {
  background: #fff;
  padding: 20px;
  border-radius: 8px;
  box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
  max-width: 400px;
  width: 100%;
  margin: 0 auto; /* Centraliza o formulário horizontalmente */
}

label {
  display: block;
  margin-bottom: 8px;
  font-weight: bold;
  color: #555;
}

input[type="text"],
input[type="number"],
input[type="file"] {
  width: 100%;
  padding: 10px;
  margin-bottom: 20px;
  border: 1px solid #ccc;
  border-radius: 4px;
  font-size: 1rem;
}

button {
  width: 100%;
  padding: 12px;
  background: #4CAF50;
  color: #fff;
  border: none;
  border-radius: 4px;
  font-size: 1rem;
  font-weight: bold;
  cursor: pointer;
  transition: background 0.3s ease;
}

button:hover {
  background: #358838;
}

button:active {
  background: #4CAF50;
}

  </style>
</head>
<body>
   <!-- Menu Superior -->
<div class="menu">
  <div>
    <a href="/catalog">Início</a>
  </div>
  <div>
    <a href="/formulario-livro">Cadastrar Livro</a>
  </div>
</div>

<div class="cadastro">
  <h1>Cadastro de Livro</h1>
  <form action="/adicionarLivro" method="POST" enctype="multipart/form-data">
    <label for="titulo">Título:</label>
    <input type="text" name="titulo" id="titulo" required>
  
    <label for="autor">Autor:</label>
    <input type="text" name="autor" id="autor" required>
  
    <label for="ano">Ano:</label>
    <input type="number" name="ano" id="ano" required>
  
    <label for="editora">Editora:</label>
    <input type="text" name="editora" id="editora" required>
  
    <label for="preco">Preço:</label>
    <input type="number" step="0.01" name="preco" id="preco" required>
  
    <label for="capa">Capa do Livro:</label>
    <input type="file" name="capa" id="capa" accept="image/*">
  
    <button type="submit">Adicionar Livro</button>
  </form>
  </div>
</body>
</html>
